#!/usr/bin/env bash

dir=$(dirname $0)
tmpfile=$(mktemp -t driller)

for test in ${dir}/test_*; do
  echo ${test}
  ./${test} &> ${tmpfile}
  if [[ $? -eq 0 ]]; then
    echo "ok"
  else
    cat ${tmpfile}
  fi
  echo "---------------------------------"
done

rm $tmpfile
